<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.eirapplets.mapper.TemperatureMapper">

    <select id="queryAllTemperatureByTime" resultType="TemperatureVO">
        SELECT u.name,datetime,temperature,place
        FROM s_temperature t
        INNER JOIN s_user u
        ON t.userid=u.id
        WHERE DATE(datetime) = #{date}
        LIMIT #{start},#{row};
    </select>

    <select id="findTotals" resultType="Integer">
        SELECT count(*) from s_temperature where DATE(datetime) = #{date};
    </select>


    <select id="queryTemperatureByUsername" resultType="TemperatureVO">
        SELECT u.name,datetime,temperature,place
        FROM s_temperature t
        INNER JOIN s_user u
        ON t.userid=u.id
        WHERE u.username = #{username}
    </select>


    <select id="getAllNoReportByDate" resultType="UserVO">
        SELECT name,username
        from s_user
        WHERE id NOT IN(
        SELECT userid
        FROM s_temperature
        WHERE DATE(datetime) = #{date}
        )
    </select>



</mapper>